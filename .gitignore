.DS_Store

backups/
__pycache__/
*.pyc

# Data lives on Google Drive, not git (640MB+). Keep code/docs/figures in git.
# data/
# *.csv
# *.zst
# *.zip

# Saved BERTopic models (large; on Drive)
models/

# Week 8 reproducibility evidence (large raw re-pull + RoBERTa re-run outputs).
# The drift/verification write-ups in docs/week8/ are the kept record; these
# raw folders are evidence only, not repo artifacts.
data/reddit_rerun/
data/roberta_rerun/
